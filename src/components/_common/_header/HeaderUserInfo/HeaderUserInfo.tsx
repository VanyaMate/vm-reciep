import React from 'react';
import css from './HeaderUserInfo.module.scss';
import Box from '@/components/_ui/_container/Box/Box.tsx';


const HeaderUserInfo = () => {
    return (
        <Box className={ css.container }>
            HeaderUserInfo
        </Box>
    );
};

export default HeaderUserInfo;